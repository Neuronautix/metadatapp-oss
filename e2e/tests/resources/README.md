# Legacy Admin Resource Specs

These specs are quarantined because they exercise the retired React-admin hash UI under `/admin#/...`.

Current Osoma metadata checks should use `/metadata` routes, as in `../navigation.spec.ts`. Keep any remaining
`/admin#/...` references here only as legacy route references.
