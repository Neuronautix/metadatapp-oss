# Architecture Decision Records

Cross-cutting architecture decisions for Metadatapp that are **not** specific to
the AI foundation (those live in `../ai-decision-records/`).

- Keep one decision per file
- Use a zero-padded numeric prefix
- Update the status when a later decision supersedes an earlier one
